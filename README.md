# jetpac-partners

Expo native app scaffolded with the TypeScript starter.

## Getting started

```bash
npm install
npm start
```

Use the platform-specific commands if needed:

```bash
npm run ios
npm run android
npm run web
```
